<div class="content">
  <p><%= product.name %></p>
  <p><%= product.price %>円</p>
  <% product.images.each do |image| %>
    <%= image_tag image.src.url %>
  <% end %>
  <%= link_to '編集', edit_product_path(product) %>
  <%# トップページから特定の投稿の編集画面に遷移できるようにするモノ %>
</div>


